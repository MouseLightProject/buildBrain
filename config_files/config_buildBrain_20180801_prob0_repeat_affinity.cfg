# make sure to add some header
outfolder = '/groups/mousebrainmicro/mousebrainmicro/cluster/re-reconstructions/2018-08-01/prob0-affinity'
skelfolder = '/groups/mousebrainmicro/mousebrainmicro/cluster/re-reconstructions/2018-08-01/prob0/'

inputh5 = '/nrs/mouselight/cluster/classifierOutputs/2018-08-01/20180801_prob0/20180801_prob0_lev-6_chunk-111_111_masked-0.h5'
h5prob = '/prob0'
h5probprops = '/prob0_props'

writefull = 1
writefrag = 1

viz = 0
debug = 0

sizethreshold = 100
# Components with less than this number of nodes are deleted

# decide crrop size from data as function of chunksize#cropSize = 1000

# overlap of running windows on skeletonization
#fullh = 15
# Should be redundant in most recent
# Some kind of padding.  Units are voxels

#    lengthThr = 10;% (um) lengthThr for each leaf branch to decide to prune
#    largesampling = 200;%(um) make it more sparse for larger segments
lengthThr = 10
# in um, fragments shorter than this are deleted

largesampling = 200
# hopfully um, threshold for when fragments are considered medium-sized vs large-sized
# sampling options are 'uni' [default] for uniform sampling (for 3D) and 'curv' for curvature weighted sampling (for 2D)
sampling = 'uni'
samplingInterval = 5
# in um, used for resampling and trimming ends of fragments at junction point

#post segmentation parameters
tag = ''
prune = 1
filterGraph = 0
graph2branch = 1

